---
name: best-practices
description: Route planning, analysis, review, and edits for e5.stack code through repo-specific best-practice references. Use this skill when a task may touch documented project standards, then select the matching reference table before acting.
---

# Best Practices

Use this skill as the entry point for repo-specific implementation standards. Match the task
surface to the tables below, read the smallest set of active references that applies, and let
those references shape the plan, review, and edits.
- Read matched references before planning, analyzing, reviewing, or editing.
- When multiple entries match, load each one; when none match, continue without a reference.
- Do not invent missing guidance or treat private authoring templates as runtime references.

### API references
| Reference | Call it when |
| --- | --- |
| `references/api/controller.md` | The task touches AdonisJS API controllers, generated controller imports, controller routes, route middleware that defines controller access, Tuyau-facing controller contracts, or Japa controller tests. |

### Web references
| Reference | Call it when |
| --- | --- |
| _None yet_ | No active Web reference exists yet. Do not load a Web reference until one is added under `references/web/`. |
